/**
 * Стартовая конфигурация нового пользователя.
 *
 * Это сид, а не константы программы: всё отсюда перезаписывается в онбординге и `/settings`,
 * а код нигде не спрашивает «а вдруг у него пятёрки» — веса всегда читаются из базы.
 * Остальные умолчания (пояс, время, 15 минут) живут в DEFAULT-ах схемы, см. migrations/0001_init.sql.
 */
export const STARTER_KETTLEBELLS = [{ weight: 5, count: 2 }] as const;
