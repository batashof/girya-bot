-- Откуда взялась демонстрация: своя присланная гифка или встроенная схема движения.
--
-- Встроенные схемы лежат в бандле воркера, но отправлять файл каждый раз незачем:
-- после первой отправки Telegram выдаёт `file_id`, и дальше уходит он (ADR-014).
-- Колонка нужна, чтобы этот кеш не выглядел как пользовательская гифка и не мешал
-- заменить схему своей: `/gif` всегда сильнее.
ALTER TABLE exercise_media ADD COLUMN source TEXT NOT NULL DEFAULT 'user'; -- user | builtin
